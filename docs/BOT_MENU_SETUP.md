# 🎯 Настройка кнопок меню в Telegram боте

Это руководство покажет как настроить удобные кнопки команд в интерфейсе вашего бота.

## 📱 Что такое кнопки меню?

Когда пользователь нажимает на иконку меню (☰) рядом с полем ввода, появляется список доступных команд. Это удобнее чем вручную набирать команды.

**До настройки:** Пользователь набирает `/export https://t.me/chat` вручную
**После настройки:** Пользователь нажимает кнопку "📊 Export Chat" из меню

---

## ⚙️ Как настроить через BotFather

### Шаг 1: Откройте BotFather

1. Откройте Telegram
2. Найдите **@BotFather** (официальный бот Telegram)
3. Запустите диалог: `/start`

### Шаг 2: Выберите команду для настройки меню

Отправьте команду:
```
/setcommands
```

### Шаг 3: Выберите вашего бота

BotFather покажет список ваших ботов. Нажмите на нужного.

### Шаг 4: Отправьте список команд

Скопируйте и отправьте следующий текст **одним сообщением**:

```
start - 🏠 Главное меню и статус
setup - ⚙️ Настройка API ключей
export - 📊 Экспорт чата в CSV
analyze - 🤖 Анализ CSV через Claude
exportanalyze - ⚡ Экспорт + анализ одной командой
help - 📖 Подробная справка
```

### Шаг 5: Готово!

BotFather ответит: "Success! Command list updated."

Теперь в боте появится кнопка меню с этими командами! ✅

---

## 📋 Альтернативный формат (с эмодзи в начале)

Если хотите эмодзи в самом начале:

```
start - 🏠 Главное меню
setup - ⚙️ Настройка бота
export - 📊 Экспорт чата
analyze - 🤖 Анализ через Claude
exportanalyze - ⚡ Экспорт + анализ
help - 📖 Помощь
```

---

## 🎨 Настройка кнопок быстрого доступа (Menu Button)

### Что это?

Кнопка, которая появляется вместо стандартной клавиатуры при открытии бота. Обычно показывает "Menu" или "Commands".

### Как настроить:

1. Откройте @BotFather
2. Отправьте: `/setmenubutton`
3. Выберите вашего бота
4. Выберите язык (например, `default` для всех языков)
5. Введите текст кнопки, например: `📱 Команды`
6. Готово!

---

## 🔘 Настройка inline-кнопок в сообщениях

Для более сложного интерфейса с кнопками в сообщениях нужно добавить код.

### Пример для команды /start

Создайте файл `bot/keyboards/main_menu.py`:

```python
from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton

def get_main_menu_keyboard() -> InlineKeyboardMarkup:
    """Главное меню с кнопками"""
    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [
            InlineKeyboardButton(text="📊 Экспорт чата", callback_data="menu_export"),
            InlineKeyboardButton(text="🤖 Анализ", callback_data="menu_analyze")
        ],
        [
            InlineKeyboardButton(text="⚡ Экспорт + Анализ", callback_data="menu_exportanalyze")
        ],
        [
            InlineKeyboardButton(text="⚙️ Настройки", callback_data="menu_setup"),
            InlineKeyboardButton(text="📖 Помощь", callback_data="menu_help")
        ]
    ])
    return keyboard


def get_export_keyboard() -> InlineKeyboardMarkup:
    """Кнопки для быстрого экспорта популярных чатов"""
    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [
            InlineKeyboardButton(text="💬 Мой чат поддержки", callback_data="export_support")
        ],
        [
            InlineKeyboardButton(text="📝 Другой чат (ввести ссылку)", callback_data="export_custom")
        ],
        [
            InlineKeyboardButton(text="⬅️ Назад в меню", callback_data="menu_main")
        ]
    ])
    return keyboard
```

### Добавьте кнопки в /start:

Обновите `bot/handlers/start.py`:

```python
from bot.keyboards.main_menu import get_main_menu_keyboard

@router.message(CommandStart())
async def cmd_start(message: Message):
    # ... существующий код ...

    await message.answer(
        "🎯 <b>Telegram Analyzer Bot</b>\n\n"
        "Выберите действие:",
        reply_markup=get_main_menu_keyboard()  # Добавили кнопки!
    )
```

### Обработчики для кнопок:

Создайте `bot/handlers/callbacks.py`:

```python
from aiogram import Router, F
from aiogram.types import CallbackQuery
from bot.keyboards.main_menu import get_main_menu_keyboard, get_export_keyboard

router = Router()

@router.callback_query(F.data == "menu_export")
async def callback_menu_export(callback: CallbackQuery):
    """Кнопка 'Экспорт чата'"""
    await callback.message.edit_text(
        "📊 <b>Экспорт чата</b>\n\n"
        "Отправьте команду:\n"
        "<code>/export ССЫЛКА_НА_ЧАТ</code>\n\n"
        "Например:\n"
        "<code>/export https://t.me/telegram</code>",
        reply_markup=get_export_keyboard()
    )
    await callback.answer()

@router.callback_query(F.data == "menu_help")
async def callback_menu_help(callback: CallbackQuery):
    """Кнопка 'Помощь'"""
    # Можно вызвать существующую команду help
    from bot.handlers.start import cmd_help
    await cmd_help(callback.message)
    await callback.answer()

@router.callback_query(F.data == "menu_main")
async def callback_menu_main(callback: CallbackQuery):
    """Возврат в главное меню"""
    await callback.message.edit_text(
        "🎯 <b>Telegram Analyzer Bot</b>\n\n"
        "Выберите действие:",
        reply_markup=get_main_menu_keyboard()
    )
    await callback.answer()
```

### Зарегистрируйте callbacks в main.py:

```python
from bot.handlers import start, export, analyze, setup, callbacks

# В функции main():
dp.include_router(start.router)
dp.include_router(callbacks.router)  # Добавьте эту строку!
dp.include_router(export.router)
# ...
```

---

## 🎯 Рекомендуемая структура меню

### Простая (только BotFather):
```
/start - Главное меню
/export - Экспорт чата
/analyze - Анализ CSV
/exportanalyze - Экспорт + анализ
/setup - Настройка
/help - Справка
```

### Продвинутая (с inline-кнопками):
```
/start
  ├─ [📊 Экспорт]     → Инструкция + примеры
  ├─ [🤖 Анализ]      → Загрузить CSV
  ├─ [⚡ Экспорт+]    → Инструкция
  ├─ [⚙️ Настройки]  → Запустить /setup
  └─ [📖 Помощь]      → Показать /help
```

---

## 💡 Советы

### ✅ Хорошие практики:
- Используйте эмодзи для наглядности
- Короткие описания команд (до 50 символов)
- Самые важные команды — первыми
- Логическая группировка (экспорт, анализ, настройки)

### ❌ Чего избегать:
- Слишком длинные описания команд
- Больше 8-10 команд в меню (перегрузка)
- Непонятные сокращения
- Команды без описания

---

## 📸 Как это выглядит

После настройки пользователь увидит:

```
Когда нажимает на иконку меню (☰):

┌─────────────────────────────┐
│ 🏠 start                    │
│ Главное меню и статус       │
├─────────────────────────────┤
│ ⚙️ setup                    │
│ Настройка API ключей        │
├─────────────────────────────┤
│ 📊 export                   │
│ Экспорт чата в CSV          │
├─────────────────────────────┤
│ 🤖 analyze                  │
│ Анализ CSV через Claude     │
├─────────────────────────────┤
│ ⚡ exportanalyze            │
│ Экспорт + анализ одной...   │
├─────────────────────────────┤
│ 📖 help                     │
│ Подробная справка           │
└─────────────────────────────┘
```

---

## 🔧 Дополнительные команды BotFather

### Изменить описание бота:
```
/setdescription - Описание в профиле бота
/setabouttext - Текст "О боте"
```

### Изменить изображение:
```
/setuserpic - Установить аватар бота
```

### Настроить inline-режим:
```
/setinline - Включить inline-режим (@botname query)
/setinlinefeedback - Статистика inline
```

---

## ✅ Чеклист настройки

- [ ] Открыл @BotFather
- [ ] Выполнил `/setcommands`
- [ ] Отправил список команд
- [ ] Проверил меню в боте
- [ ] (Опционально) Настроил `/setmenubutton`
- [ ] (Опционально) Добавил inline-кнопки в код

---

## 🚀 Готово!

Теперь ваш бот имеет удобное меню команд!

Пользователи могут:
- ✅ Видеть все доступные команды одним нажатием
- ✅ Быстро выбирать нужную команду
- ✅ Не запоминать названия команд

**Следующий шаг:** Настройте бот для работы 24/7 на сервере (см. `SERVER_DEPLOYMENT.md`)
