# 🔧 Решение проблем (Troubleshooting)

Руководство по решению частых проблем при работе с Telegram Analyzer Bot.

---

## 📋 Содержание

1. [Ошибки экспорта чатов](#ошибки-экспорта-чатов)
2. [Ошибки авторизации](#ошибки-авторизации)
3. [Ошибки анализа через Claude](#ошибки-анализа-через-claude)
4. [Ошибки настройки](#ошибки-настройки)
5. [Общие проблемы](#общие-проблемы)

---

## 🚫 Ошибки экспорта чатов

### ❌ "Cannot get entity from a channel (or group) that you are not part of"

**Описание:** Вы пытаетесь экспортировать чат, но не являетесь его участником.

**Причина:** Telegram API не позволяет получать информацию о чатах, где вы не состоите.

**Решение:**

1. Откройте ссылку на чат в Telegram
2. Нажмите "Присоединиться" / "Join"
3. После вступления повторите команду экспорта

```bash
# Пример
/export https://t.me/+ABC123xyz
```

**Важно:** Это ограничение Telegram, а не бота!

---

### ❌ "Session expired" / "Сессия истекла"

**Описание:** Ваша сессия Telegram истекла.

**Причина:** Прошло много времени с момента авторизации, или вы вышли из аккаунта на другом устройстве.

**Решение:**

1. Запустите `/setup` в боте
2. Пройдите авторизацию заново
3. Отсканируйте QR-код в Telegram
4. Повторите экспорт

---

### ❌ "File too large" / "Файл слишком большой"

**Описание:** CSV файл превышает лимиты Telegram (2GB для документов).

**Причина:** Чат содержит очень много сообщений.

**Решение:**

**Вариант 1:** Экспортируйте по частям:
- Используйте фильтры по датам
- Разбейте на периоды (по месяцам/годам)

**Вариант 2:** Экспортируйте меньше сообщений:
- По умолчанию лимит 10,000 сообщений
- Этого достаточно для большинства анализов

---

## 🔐 Ошибки авторизации

### ❌ "API_ID or API_HASH not configured"

**Описание:** Не настроены Telegram API ключи.

**Решение:**

1. Перейдите на https://my.telegram.org
2. Войдите под своим номером телефона
3. Откройте "API development tools"
4. Создайте приложение (если нет)
5. Скопируйте API ID и API Hash
6. Запустите `/setup` в боте
7. Введите полученные ключи

---

### ❌ "QR code expired" / "QR-код истек"

**Описание:** QR-код для авторизации истек (5 минут).

**Решение:**

1. Запустите `/setup` заново
2. Получите новый QR-код
3. Быстрее отсканируйте его в Telegram

**Совет:** Держите Telegram открытым перед запуском `/setup`

---

### ❌ "Phone number banned" / "Номер телефона заблокирован"

**Описание:** Ваш номер заблокирован в Telegram API.

**Причина:** Возможное нарушение правил использования API.

**Решение:**

1. Проверьте статус аккаунта в Telegram
2. Обратитесь в поддержку Telegram (если блокировка ошибочна)
3. Используйте другой аккаунт

---

## 🤖 Ошибки анализа через Claude

### ❌ "Claude API key not configured"

**Описание:** Не настроен Claude API ключ.

**Решение:**

1. Получите API ключ на https://console.anthropic.com
2. Запустите `/setup` в боте
3. Введите Claude API ключ
4. Повторите анализ

---

### ❌ "Rate limit exceeded" / "Превышен лимит запросов"

**Описание:** Слишком много запросов к Claude API.

**Причина:** Ограничения API плана.

**Решение:**

**Вариант 1:** Подождите:
- Лимиты сбрасываются через время
- Обычно 1-5 минут

**Вариант 2:** Обновите план:
- Перейдите на платный план Claude API
- Увеличьте лимиты запросов

---

### ❌ "Invalid API key" / "Неверный API ключ"

**Описание:** Claude API ключ недействителен.

**Причина:**
- Ключ скопирован неполностью
- Ключ был отозван
- Истек срок действия

**Решение:**

1. Проверьте ключ на https://console.anthropic.com
2. Создайте новый ключ (если нужно)
3. Обновите через `/setup`

---

### ❌ "CSV file too large for analysis"

**Описание:** CSV файл слишком большой для анализа.

**Причина:** Claude API имеет лимиты на размер контекста.

**Решение:**

**Автоматическое решение:**
- Бот автоматически ограничивает до 5,000 строк
- Для анализа используются самые последние сообщения

**Ручное решение:**
- Экспортируйте конкретный период
- Используйте меньший лимит сообщений

---

## ⚙️ Ошибки настройки

### ❌ "BOT_TOKEN not configured"

**Описание:** Не настроен токен бота.

**Решение:**

1. Получите токен у @BotFather в Telegram
2. Создайте `data/.env` файл:
   ```
   BOT_TOKEN=your_token_here
   ```
3. Перезапустите бота

---

### ❌ "Database error" / "Ошибка базы данных"

**Описание:** Проблемы с базой данных SQLite.

**Причина:**
- Поврежденная база данных
- Недостаточно прав доступа
- Заполнен диск

**Решение:**

**Вариант 1:** Проверьте права:
```bash
ls -la data/users.db
chmod 644 data/users.db
```

**Вариант 2:** Пересоздайте БД:
```bash
# ВНИМАНИЕ: Это удалит все данные!
rm data/users.db
python main.py --bot
```

**Вариант 3:** Проверьте место на диске:
```bash
df -h
```

---

## 🐛 Общие проблемы

### ❌ "Bot not responding" / "Бот не отвечает"

**Проверки:**

1. **Бот запущен?**
   ```bash
   # Linux
   ps aux | grep "python.*main.py"

   # Windows
   tasklist | findstr python
   ```

2. **Токен правильный?**
   - Проверьте `data/.env`
   - Токен должен содержать `:`
   - Минимум 20 символов после `:`

3. **Интернет есть?**
   ```bash
   ping api.telegram.org
   ```

4. **Логи показывают ошибки?**
   ```bash
   tail -f logs/bot.log
   # или
   journalctl -u telegram-analyzer -f
   ```

---

### ❌ "Connection timeout" / "Тайм-аут соединения"

**Описание:** Не удается подключиться к Telegram.

**Причины:**
- Проблемы с интернетом
- Блокировка Telegram в стране
- Проблемы с DNS

**Решение:**

**Вариант 1:** Проверьте интернет:
```bash
ping 8.8.8.8
```

**Вариант 2:** Используйте VPN/прокси (если Telegram заблокирован)

**Вариант 3:** Смените DNS:
```bash
# Google DNS
echo "nameserver 8.8.8.8" | sudo tee /etc/resolv.conf
```

---

### ❌ "Permission denied" / "Доступ запрещен"

**Описание:** Недостаточно прав доступа.

**Решение:**

```bash
# Дать права на директорию
chmod -R 755 ~/Telegram_Analyzer

# Дать права на запуск
chmod +x main.py

# Проверить владельца
ls -la
chown -R $USER:$USER ~/Telegram_Analyzer
```

---

### ❌ "Module not found" / "Модуль не найден"

**Описание:** Отсутствуют Python зависимости.

**Решение:**

```bash
# Активировать виртуальное окружение
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Установить зависимости
pip install -r requirements.txt

# Проверить установку
pip list
```

---

## 📱 Как получить подробные логи

### Для пользователя:

Логи задач отображаются в боте автоматически:
- ✅ Успешные задачи
- ❌ Ошибки с описанием
- ⏳ Статус выполнения

### Для администратора:

```bash
# Системные логи (если используется systemd)
sudo journalctl -u telegram-analyzer -n 100

# Логи в реальном времени
sudo journalctl -u telegram-analyzer -f

# Логи приложения
tail -f logs/bot.log
tail -f logs/error.log

# Логи с временем
grep "ERROR" logs/bot.log | tail -20
```

---

## 🆘 Когда обращаться к администратору

Обратитесь к администратору, если:

1. **Ошибка повторяется** после всех попыток решения
2. **Бот постоянно падает** или не запускается
3. **База данных повреждена** и не восстанавливается
4. **Проблемы с сервером** (если бот на VPS)
5. **Нужна помощь с настройкой** API ключей

**Что предоставить администратору:**

- Описание проблемы
- Скриншот ошибки из бота
- Логи (если есть доступ)
- Что уже пробовали

---

## 📚 Полезные ссылки

- [Telegram API Documentation](https://core.telegram.org/api)
- [Telethon Documentation](https://docs.telethon.dev/)
- [Claude API Documentation](https://docs.anthropic.com/)
- [Python aiogram Documentation](https://docs.aiogram.dev/)

---

## ✅ Чеклист диагностики

Перед обращением за помощью проверьте:

- [ ] Бот запущен
- [ ] BOT_TOKEN настроен
- [ ] Интернет соединение работает
- [ ] Вы авторизованы через /setup
- [ ] Вы состоите в чате который экспортируете
- [ ] Claude API ключ настроен (для анализа)
- [ ] Логи не показывают критических ошибок
- [ ] Перезапуск бота не помог
- [ ] Версия Python 3.11+
- [ ] Зависимости установлены (requirements.txt)

---

**Если проблема не решена, создайте Issue на GitHub с описанием проблемы и логами.**
