# 🚀 Деплой Telegram Analyzer на Render.com

Полное руководство по развертыванию Telegram Analyzer Bot на платформе Render.

## 📋 Содержание

1. [Почему Render?](#почему-render)
2. [Требования](#требования)
3. [Быстрый старт](#быстрый-старт)
4. [Детальная настройка](#детальная-настройка)
5. [Настройка переменных окружения](#настройка-переменных-окружения)
6. [Мониторинг и логи](#мониторинг-и-логи)
7. [Troubleshooting](#troubleshooting)
8. [Обновление бота](#обновление-бота)

---

## 🎯 Почему Render?

**Преимущества:**
- ✅ **Бесплатный tier** с 750 часами в месяц
- ✅ **Автоматический деплой** при push в GitHub
- ✅ **Встроенные логи** и мониторинг
- ✅ **Docker support** из коробки
- ✅ **Background Workers** для ботов
- ✅ **Persistent Disks** для данных
- ✅ **Нет холодного старта** (в отличие от Heroku)
- ✅ **Бесплатный SSL** и DDoS защита

**Сравнение с Railway:**
| Функция | Render | Railway |
|---------|--------|---------|
| Бесплатный план | 750 часов/месяц | $5 кредит |
| Docker | ✅ | ✅ |
| Auto-deploy | ✅ | ✅ |
| Логи | ✅ Отличные | ✅ Хорошие |
| Persistent storage | ✅ Бесплатно 1GB | ✅ Платно |
| Регионы | 🌍 Много | 🌍 Много |

---

## ⚙️ Требования

### 1. GitHub Repository
Убедитесь, что ваш код в GitHub репозитории:
```bash
git remote -v
# Должен показать ваш GitHub repo
```

### 2. Telegram Bot Token
Получите токен у [@BotFather](https://t.me/BotFather):
```
/newbot
Следуйте инструкциям
Скопируйте токен: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

### 3. Аккаунт на Render
Зарегистрируйтесь на [render.com](https://render.com)
- Используйте GitHub для входа (рекомендуется)
- Это автоматически свяжет ваши репозитории

---

## ⚡ Быстрый старт

### Вариант 1: Blueprint (Рекомендуется)

1. **Нажмите кнопку Deploy:**

   [![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy)

2. **Заполните переменные:**
   - `BOT_TOKEN` - ваш токен от BotFather

3. **Нажмите "Create"** - готово! 🎉

### Вариант 2: Ручная настройка

#### Шаг 1: Создать новый сервис

1. Откройте [Render Dashboard](https://dashboard.render.com)
2. Нажмите **"New +"** → **"Background Worker"**
3. Выберите ваш GitHub репозиторий

#### Шаг 2: Настроить сервис

```yaml
Name: telegram-analyzer-bot
Environment: Docker
Region: Oregon (или Frankfurt для Европы)
Branch: main (или ваша основная ветка)
```

#### Шаг 3: Докер конфигурация

Render автоматически найдет `render.yaml` и применит настройки:
```yaml
✅ Dockerfile: ./Dockerfile
✅ Docker Context: .
✅ Build Command: автоматически
✅ Start Command: python main.py --bot
```

#### Шаг 4: Переменные окружения

Добавьте в Environment Variables:

| Ключ | Значение | Обязательно |
|------|----------|-------------|
| `BOT_TOKEN` | `1234567890:ABC...` | ✅ Да |
| `PYTHONUNBUFFERED` | `1` | ✅ Авто |
| `PYTHONDONTWRITEBYTECODE` | `1` | ✅ Авто |

**Примечание:** Переменные с "Авто" уже установлены в `render.yaml`.

#### Шаг 5: Создать и запустить

1. Нажмите **"Create Background Worker"**
2. Render начнет деплой (обычно 3-5 минут)
3. Следите за логами в реальном времени

---

## 🔧 Детальная настройка

### Использование render.yaml

Проект уже содержит `render.yaml` с оптимальными настройками:

```yaml
services:
  - type: worker
    name: telegram-analyzer-bot
    env: docker
    dockerfilePath: ./Dockerfile
    region: oregon
    plan: free

    envVars:
      - key: BOT_TOKEN
        sync: false  # Установить вручную
```

### Настройка региона

Выберите ближайший регион для лучшей производительности:

- 🇺🇸 **Oregon** (us-west) - по умолчанию
- 🇺🇸 **Ohio** (us-east)
- 🇪🇺 **Frankfurt** (europe-west) - для Европы и России
- 🇸🇬 **Singapore** (asia-southeast) - для Азии

Измените в `render.yaml`:
```yaml
region: frankfurt  # для Европы
```

### Persistent Disk (опционально)

Для сохранения данных между деплоями:

1. Раскомментируйте в `render.yaml`:
```yaml
disk:
  name: bot-data
  mountPath: /app/data
  sizeGB: 1  # Бесплатно до 1GB
```

2. Данные в `/app/data` будут сохраняться:
   - База данных SQLite
   - Логи
   - Экспортированные файлы

**Важно:** Без диска данные удаляются при каждом деплое!

---

## 🌍 Настройка переменных окружения

### Обязательные переменные

#### BOT_TOKEN
```bash
Описание: Токен Telegram бота
Где получить: @BotFather
Формат: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
Обязательно: Да
```

### Опциональные переменные

#### CLAUDE_API_KEY (опционально)
```bash
Описание: Глобальный API ключ Claude (можно настроить per-user через /setup)
Где получить: console.anthropic.com
Формат: sk-ant-api03-...
Обязательно: Нет
```

### Как добавить переменные

**Через Dashboard:**
1. Откройте сервис в Render Dashboard
2. Перейдите в **Environment**
3. Нажмите **"Add Environment Variable"**
4. Введите `Key` и `Value`
5. Нажмите **"Save Changes"**

**Через render.yaml:**
```yaml
envVars:
  - key: MY_VARIABLE
    value: my_value

  - key: SECRET_KEY
    sync: false  # Установить вручную в Dashboard
```

---

## 📊 Мониторинг и логи

### Просмотр логов

1. **Через Dashboard:**
   - Откройте сервис
   - Перейдите в **Logs**
   - Логи обновляются в реальном времени

2. **Через CLI:**
```bash
# Установить Render CLI
npm install -g @render-cli/render

# Войти
render login

# Смотреть логи
render logs --service telegram-analyzer-bot --tail
```

### Важные логи

**Успешный запуск:**
```
✅ База данных инициализирована
✅ FSM storage настроен
✅ Обработчики зарегистрированы
✅ Бот запущен и готов к работе!
Run polling for bot @your_bot id=123456789
```

**Ошибки:**
```
❌ BOT_TOKEN не настроен!
❌ Ошибка инициализации БД
```

### Метрики

Render предоставляет:
- **CPU Usage** - использование процессора
- **Memory Usage** - использование памяти
- **Restart Count** - количество перезапусков
- **Uptime** - время работы

Доступны в разделе **Metrics**.

---

## 🔍 Troubleshooting

### Конфликт: "Multiple bot instances running"

**Проблема:** `TelegramConflictError: Conflict: terminated by other getUpdates request`

**Причина:** Запущено несколько экземпляров бота с одним токеном.

**Решение:**
1. Проверьте активные деплои:
   - Railway (railway.app)
   - Render (dashboard.render.com)
   - Локальный запуск
2. **Остановите все кроме одного**
3. Подождите 10-20 секунд
4. Проверьте логи - должно быть "Бот запущен и готов к работе!"

**Детали:** См. [BOT_CONFLICT_RESOLUTION.md](BOT_CONFLICT_RESOLUTION.md)

---

### Бот не отвечает

**Проблема:** Бот онлайн, но не отвечает на команды.

**Решение:**
```bash
# Проверьте логи на ошибки
# Убедитесь что BOT_TOKEN правильный
# Проверьте что бот запущен:
grep "Бот запущен и готов" logs
```

### Ошибка "BOT_TOKEN не настроен"

**Проблема:** Бот не может найти токен.

**Решение:**
1. Откройте **Environment** в Dashboard
2. Добавьте `BOT_TOKEN` с вашим токеном
3. Нажмите **Manual Deploy** → **Deploy latest commit**

### Постоянные перезапуски

**Проблема:** Бот перезапускается каждые несколько минут.

**Решение:**
```bash
# Проверьте логи на панику или ошибки
# Возможные причины:
- Неправильный BOT_TOKEN
- Нехватка памяти (обновите план)
- Ошибка в коде (проверьте последний коммит)
```

### Out of Memory

**Проблема:** `MemoryError` или `OOMKilled` в логах.

**Решение:**
1. Обновите план с Free на Starter ($7/месяц)
2. Или оптимизируйте код:
   - Уменьшите `MAX_CSV_ROWS` в `services/analyzer.py`
   - Очищайте старые файлы

### База данных пропадает после деплоя

**Проблема:** Данные пользователей теряются.

**Решение:**
Включите Persistent Disk:
```yaml
disk:
  name: bot-data
  mountPath: /app/data
  sizeGB: 1
```

### Медленный деплой

**Проблема:** Деплой занимает больше 10 минут.

**Решение:**
- Это нормально для первого деплоя
- Последующие деплои используют кэш и быстрее
- Если долго - проверьте размер Docker образа

---

## 🔄 Обновление бота

### Автоматическое обновление

Render автоматически деплоит при push в GitHub:

```bash
# Внесите изменения
git add .
git commit -m "feat: new feature"
git push origin main

# Render автоматически:
1. Обнаружит изменения
2. Пересоберет Docker образ
3. Задеплоит новую версию
```

### Ручное обновление

1. Откройте сервис в Dashboard
2. Нажмите **Manual Deploy**
3. Выберите **Deploy latest commit**
4. Или **Clear build cache & deploy** для полной пересборки

### Откат на предыдущую версию

1. Откройте **Events** в Dashboard
2. Найдите успешный деплой
3. Нажмите **"Rollback to this deploy"**

---

## 💡 Лучшие практики

### Безопасность

✅ **DO:**
- Используйте Environment Variables для секретов
- Включите GitHub Branch Protection
- Регулярно обновляйте зависимости

❌ **DON'T:**
- Не коммитьте `.env` файлы
- Не храните токены в коде
- Не используйте слабые пароли

### Производительность

- Используйте ближайший регион
- Включите Persistent Disk для данных
- Мониторьте использование памяти
- Оптимизируйте размер Docker образа

### Мониторинг

- Настройте уведомления в Render
- Используйте логирование
- Регулярно проверяйте метрики
- Настройте health checks (опционально)

---

## 📞 Поддержка

### Документация Render
- [Render Docs](https://render.com/docs)
- [Background Workers](https://render.com/docs/background-workers)
- [Docker на Render](https://render.com/docs/docker)

### Сообщество
- [Render Community](https://community.render.com)
- [Discord](https://discord.gg/render)

### Проект Telegram Analyzer
- [GitHub Issues](https://github.com/Damir-Dev-AI-Creator/Telegram_Analyzer/issues)
- [Документация](../README.md)

---

## 🎉 Готово!

Ваш Telegram Analyzer Bot теперь работает на Render!

**Следующие шаги:**
1. Откройте бота в Telegram
2. Отправьте `/start`
3. Настройте с помощью `/setup`
4. Начните экспортировать чаты!

**Полезные ссылки:**
- [Render Dashboard](https://dashboard.render.com)
- [Логи бота](https://dashboard.render.com/logs)
- [Настройки окружения](https://dashboard.render.com/env)

Приятного использования! 🚀
