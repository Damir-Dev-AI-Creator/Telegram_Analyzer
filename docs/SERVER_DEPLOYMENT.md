# 🚀 Развертывание бота на сервере 24/7

Это руководство покажет как запустить Telegram Analyzer Bot на сервере, чтобы он работал постоянно без необходимости держать компьютер включенным.

---

## 📋 Содержание

1. [Выбор сервера](#выбор-сервера)
2. [Быстрый старт (Ubuntu/Debian)](#быстрый-старт-ubuntudebian)
3. [Настройка systemd (автозапуск)](#настройка-systemd-автозапуск)
4. [Альтернативные методы](#альтернативные-методы)
5. [Мониторинг и логи](#мониторинг-и-логи)
6. [Обновление бота](#обновление-бота)
7. [Решение проблем](#решение-проблем)

---

## 🌐 Выбор сервера

### Рекомендуемые VPS провайдеры:

| Провайдер | Цена/месяц | Память | Подходит для |
|-----------|------------|--------|--------------|
| **DigitalOcean** | $6 | 1GB RAM | ✅ Отлично для начала |
| **Hetzner** | €4.5 | 4GB RAM | ✅ Лучшее соотношение цена/качество |
| **Linode** | $5 | 1GB RAM | ✅ Хорошая производительность |
| **Vultr** | $6 | 2GB RAM | ✅ Много локаций |
| **Contabo** | €5 | 8GB RAM | ✅ Дешево, много ресурсов |

### Минимальные требования:
- **RAM:** 512MB (рекомендуется 1GB+)
- **CPU:** 1 ядро
- **Диск:** 10GB SSD
- **ОС:** Ubuntu 22.04 LTS / Debian 11+
- **Сеть:** Стабильный интернет

### 🆓 Бесплатные варианты:

**1. Oracle Cloud (Always Free Tier)**
- ✅ 1GB RAM бесплатно навсегда
- ✅ 2 виртуальные машины
- ❌ Сложная регистрация

**2. Google Cloud Platform (90 дней $300)**
- ✅ Пробный период 3 месяца
- ❌ После окончания нужна оплата

**3. AWS EC2 (12 месяцев free tier)**
- ✅ t2.micro бесплатно 1 год
- ❌ Ограничения по трафику

---

## ⚡ Быстрый старт (Ubuntu/Debian)

### Шаг 1: Подключение к серверу

```bash
# Подключитесь по SSH (замените IP и username)
ssh root@your_server_ip

# Или если используется пользователь ubuntu:
ssh ubuntu@your_server_ip
```

### Шаг 2: Обновление системы

```bash
# Обновить список пакетов
sudo apt update

# Обновить установленные пакеты
sudo apt upgrade -y
```

### Шаг 3: Установка зависимостей

```bash
# Установить Python 3.11+, git, и другие зависимости
sudo apt install -y python3 python3-pip python3-venv git

# Проверить версию Python (должна быть 3.11+)
python3 --version
```

### Шаг 4: Клонирование репозитория

```bash
# Создать директорию для проектов
mkdir -p ~/projects
cd ~/projects

# Клонировать ваш репозиторий
git clone https://github.com/YOUR_USERNAME/Telegram_Analyzer.git
cd Telegram_Analyzer
```

### Шаг 5: Настройка виртуального окружения

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать виртуальное окружение
source venv/bin/activate

# Обновить pip
pip install --upgrade pip

# Установить зависимости
pip install -r requirements.txt
```

### Шаг 6: Настройка конфигурации

```bash
# Создать директорию для данных
mkdir -p data

# Создать .env файл
nano data/.env
```

Добавьте в файл:
```bash
# Bot Token от @BotFather
BOT_TOKEN=your_bot_token_here
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 7: Первый запуск (тест)

```bash
# Запустить бота в тестовом режиме
python main.py --bot

# Если всё работает, остановите: Ctrl+C
```

---

## 🔄 Настройка systemd (автозапуск)

Systemd обеспечит автоматический запуск бота при перезагрузке сервера и перезапуск при сбоях.

### Шаг 1: Создать systemd service файл

```bash
sudo nano /etc/systemd/system/telegram-analyzer.service
```

### Шаг 2: Добавить конфигурацию

Вставьте следующий текст (замените пути на свои):

```ini
[Unit]
Description=Telegram Analyzer Bot
After=network.target

[Service]
Type=simple
User=YOUR_USERNAME
WorkingDirectory=/home/YOUR_USERNAME/projects/Telegram_Analyzer
Environment="PATH=/home/YOUR_USERNAME/projects/Telegram_Analyzer/venv/bin"
ExecStart=/home/YOUR_USERNAME/projects/Telegram_Analyzer/venv/bin/python main.py --bot

# Автоматический перезапуск при сбое
Restart=always
RestartSec=10

# Логирование
StandardOutput=append:/home/YOUR_USERNAME/projects/Telegram_Analyzer/logs/bot.log
StandardError=append:/home/YOUR_USERNAME/projects/Telegram_Analyzer/logs/error.log

[Install]
WantedBy=multi-user.target
```

**Важно:** Замените `YOUR_USERNAME` на ваше имя пользователя!

Узнать имя пользователя:
```bash
whoami
```

Сохраните файл: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 3: Создать директорию для логов

```bash
mkdir -p ~/projects/Telegram_Analyzer/logs
```

### Шаг 4: Включить и запустить сервис

```bash
# Перезагрузить конфигурацию systemd
sudo systemctl daemon-reload

# Включить автозапуск при загрузке системы
sudo systemctl enable telegram-analyzer

# Запустить бота
sudo systemctl start telegram-analyzer

# Проверить статус
sudo systemctl status telegram-analyzer
```

### Шаг 5: Проверить работу

Если всё настроено правильно, вы увидите:
```
● telegram-analyzer.service - Telegram Analyzer Bot
   Loaded: loaded (/etc/systemd/system/telegram-analyzer.service; enabled)
   Active: active (running) since ...
```

---

## 🛠 Управление сервисом

### Основные команды:

```bash
# Запустить бота
sudo systemctl start telegram-analyzer

# Остановить бота
sudo systemctl stop telegram-analyzer

# Перезапустить бота
sudo systemctl restart telegram-analyzer

# Статус бота
sudo systemctl status telegram-analyzer

# Посмотреть логи
sudo journalctl -u telegram-analyzer -f

# Последние 100 строк логов
sudo journalctl -u telegram-analyzer -n 100

# Логи с конкретного времени
sudo journalctl -u telegram-analyzer --since "1 hour ago"
```

---

## 🔄 Альтернативные методы

### Метод 1: Screen (простой)

Screen позволяет запускать процессы в фоновом режиме.

```bash
# Установить screen
sudo apt install screen -y

# Создать новую сессию
screen -S telegram_bot

# Активировать виртуальное окружение и запустить бота
cd ~/projects/Telegram_Analyzer
source venv/bin/activate
python main.py --bot

# Отключиться от сессии (бот продолжит работу)
# Нажмите: Ctrl+A, затем D

# Вернуться к сессии
screen -r telegram_bot

# Список всех сессий
screen -ls

# Завершить сессию (остановить бота)
screen -S telegram_bot -X quit
```

### Метод 2: tmux (продвинутый)

Tmux похож на screen, но с дополнительными функциями.

```bash
# Установить tmux
sudo apt install tmux -y

# Создать новую сессию
tmux new -s telegram_bot

# Запустить бота
cd ~/projects/Telegram_Analyzer
source venv/bin/activate
python main.py --bot

# Отключиться от сессии
# Нажмите: Ctrl+B, затем D

# Вернуться к сессии
tmux attach -t telegram_bot

# Список сессий
tmux ls

# Завершить сессию
tmux kill-session -t telegram_bot
```

### Метод 3: nohup (самый простой)

```bash
# Запустить в фоне
cd ~/projects/Telegram_Analyzer
source venv/bin/activate
nohup python main.py --bot > logs/bot.log 2>&1 &

# Найти процесс
ps aux | grep "python main.py"

# Остановить (замените PID на реальный)
kill PID
```

---

## 📊 Мониторинг и логи

### Просмотр логов в реальном времени:

```bash
# Системные логи (через systemd)
sudo journalctl -u telegram-analyzer -f

# Логи приложения (если настроены в service)
tail -f ~/projects/Telegram_Analyzer/logs/bot.log

# Логи ошибок
tail -f ~/projects/Telegram_Analyzer/logs/error.log
```

### Мониторинг ресурсов:

```bash
# Использование CPU и памяти
htop

# Или базовый top
top

# Статистика по процессу бота
ps aux | grep python
```

### Настройка ротации логов:

Чтобы логи не занимали всё место:

```bash
sudo nano /etc/logrotate.d/telegram-analyzer
```

Добавьте:
```
/home/YOUR_USERNAME/projects/Telegram_Analyzer/logs/*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
}
```

---

## 🔄 Обновление бота

### Метод 1: Через systemd

```bash
# Остановить бота
sudo systemctl stop telegram-analyzer

# Перейти в директорию проекта
cd ~/projects/Telegram_Analyzer

# Получить обновления
git pull origin main

# Активировать виртуальное окружение
source venv/bin/activate

# Обновить зависимости (если изменились)
pip install -r requirements.txt --upgrade

# Запустить бота
sudo systemctl start telegram-analyzer

# Проверить статус
sudo systemctl status telegram-analyzer
```

### Метод 2: Через screen/tmux

```bash
# Подключиться к сессии
screen -r telegram_bot
# или
tmux attach -t telegram_bot

# Остановить бота: Ctrl+C

# Обновить код
git pull origin main
pip install -r requirements.txt --upgrade

# Запустить снова
python main.py --bot

# Отключиться: Ctrl+A D (screen) или Ctrl+B D (tmux)
```

---

## 🔥 Firewall (опционально, для безопасности)

### Настройка UFW:

```bash
# Установить UFW
sudo apt install ufw -y

# Разрешить SSH (ВАЖНО! Иначе потеряете доступ)
sudo ufw allow ssh
sudo ufw allow 22/tcp

# Включить firewall
sudo ufw enable

# Проверить статус
sudo ufw status
```

---

## ⚠️ Решение проблем

### Бот не запускается:

1. **Проверьте логи:**
```bash
sudo journalctl -u telegram-analyzer -n 50
```

2. **Проверьте статус:**
```bash
sudo systemctl status telegram-analyzer
```

3. **Проверьте права на файлы:**
```bash
ls -la ~/projects/Telegram_Analyzer/
```

### "ModuleNotFoundError":

Зависимости не установлены:
```bash
cd ~/projects/Telegram_Analyzer
source venv/bin/activate
pip install -r requirements.txt
```

### "Permission denied":

Проблема с правами доступа:
```bash
# Дать права на директорию
chmod -R 755 ~/projects/Telegram_Analyzer

# Проверить владельца
ls -la ~/projects/
```

### Бот постоянно перезапускается:

Проверьте ошибки в коде:
```bash
# Попробуйте запустить вручную
cd ~/projects/Telegram_Analyzer
source venv/bin/activate
python main.py --bot
```

### Нет BOT_TOKEN:

```bash
# Проверьте .env файл
cat ~/projects/Telegram_Analyzer/data/.env

# Если пустой, создайте заново
nano ~/projects/Telegram_Analyzer/data/.env
```

---

## 📱 Проверка работы

После запуска на сервере:

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Если бот отвечает — всё работает! ✅

---

## 🔐 Безопасность

### Рекомендации:

1. **Используйте SSH ключи вместо паролей:**
```bash
# На локальном компьютере сгенерируйте ключ
ssh-keygen -t ed25519

# Скопируйте на сервер
ssh-copy-id user@server_ip
```

2. **Отключите вход по паролю:**
```bash
sudo nano /etc/ssh/sshd_config
# Найдите и измените:
# PasswordAuthentication no

sudo systemctl restart sshd
```

3. **Регулярно обновляйте систему:**
```bash
sudo apt update && sudo apt upgrade -y
```

4. **Не храните секреты в коде:**
   - Используйте `.env` файл
   - Добавьте `.env` в `.gitignore`

---

## ✅ Чеклист развертывания

- [ ] Выбрал и настроил VPS
- [ ] Подключился по SSH
- [ ] Обновил систему (`apt update && apt upgrade`)
- [ ] Установил Python 3.11+ и git
- [ ] Склонировал репозиторий
- [ ] Создал виртуальное окружение
- [ ] Установил зависимости
- [ ] Создал `data/.env` с BOT_TOKEN
- [ ] Настроил systemd service
- [ ] Запустил бота (`systemctl start telegram-analyzer`)
- [ ] Включил автозапуск (`systemctl enable telegram-analyzer`)
- [ ] Проверил работу в Telegram
- [ ] Настроил мониторинг логов
- [ ] (Опционально) Настроил firewall

---

## 🎯 Готово!

Теперь ваш бот работает 24/7! 🎉

**Что дальше:**
- Настройте кнопки меню (см. `BOT_MENU_SETUP.md`)
- Мониторьте логи периодически
- Обновляйте бота при добавлении новых функций

**Полезные команды для заметок:**
```bash
# Статус бота
sudo systemctl status telegram-analyzer

# Логи в реальном времени
sudo journalctl -u telegram-analyzer -f

# Перезапуск
sudo systemctl restart telegram-analyzer

# Обновление
cd ~/projects/Telegram_Analyzer && git pull && sudo systemctl restart telegram-analyzer
```

---

## 📚 Дополнительные ресурсы

- [DigitalOcean Tutorials](https://www.digitalocean.com/community/tutorials)
- [Ubuntu Server Guide](https://ubuntu.com/server/docs)
- [Systemd Documentation](https://systemd.io/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

**Вопросы или проблемы?** Проверьте логи и раздел "Решение проблем" выше!
