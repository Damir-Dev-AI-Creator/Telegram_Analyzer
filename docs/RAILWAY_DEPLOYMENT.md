# 🚂 Развертывание на Railway - Пошаговая инструкция

Railway - это современная PaaS платформа для быстрого развертывания приложений. Идеально подходит для Telegram ботов!

---

## 🎯 Почему Railway?

✅ **Бесплатный тестовый период** - $5 кредитов при регистрации
✅ **Простота** - развертывание за 5 минут
✅ **Автоматические обновления** - git push и готово
✅ **Бесплатный SSL** - безопасное соединение
✅ **Логи в реальном времени** - удобная отладка
✅ **Автоматический рестарт** - бот восстанавливается при сбое

---

## 💰 Стоимость

### Free Trial (тестовый период):
- **$5 кредитов** при регистрации
- Хватит на ~2-3 недели работы бота
- Не требуется карта при регистрации

### После триала:
- **$5/месяц** - минимальный план (Hobby)
- Неограниченное время работы
- 8GB RAM, 8 vCPU

### Альтернативы Railway:
- **Render** - $0/месяц (ограничения: спящий режим через 15 мин бездействия)
- **Fly.io** - $0/месяц (ограничения: 3 приложения бесплатно)
- **Koyeb** - $0/месяц (ограничения: 1 сервис бесплатно)

---

## 📋 Подготовка

### 1. Убедитесь что у вас есть:
- [x] Аккаунт GitHub
- [x] Репозиторий с кодом бота
- [x] BOT_TOKEN от [@BotFather](https://t.me/BotFather)

### 2. Файлы проекта (уже созданы):
- ✅ `Procfile` - команда запуска
- ✅ `Dockerfile` - конфигурация контейнера
- ✅ `railway.toml` - настройки Railway
- ✅ `.dockerignore` - исключаемые файлы
- ✅ `requirements.txt` - зависимости Python

---

## 🚀 Развертывание (5 шагов)

### Шаг 1: Регистрация на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите **"Start a New Project"**
3. Войдите через **GitHub**
4. Подтвердите доступ к репозиториям

### Шаг 2: Создание проекта

1. В панели Railway нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите ваш репозиторий `Telegram_Analyzer`
4. Нажмите **"Deploy Now"**

Railway автоматически:
- ✅ Обнаружит `Dockerfile`
- ✅ Соберет Docker образ
- ✅ Запустит контейнер

### Шаг 3: Настройка переменных окружения

**Важно!** Бот не запустится без BOT_TOKEN.

1. В панели Railway откройте ваш проект
2. Перейдите на вкладку **"Variables"**
3. Добавьте переменную:
   ```
   BOT_TOKEN=ваш_токен_от_BotFather
   ```
4. Нажмите **"Add"**

### Шаг 4: Проверка деплоя

1. Перейдите на вкладку **"Deployments"**
2. Дождитесь статуса **"Success"** (обычно 2-3 минуты)
3. Перейдите на вкладку **"Logs"**
4. Убедитесь что видите:
   ```
   ✅ Бот запущен и готов к работе!
   ```

### Шаг 5: Тестирование

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. **Если бот отвечает - готово!** 🎉

---

## 📊 Мониторинг

### Просмотр логов в реальном времени:

1. Откройте проект в Railway
2. Вкладка **"Logs"**
3. Видите всё что происходит в боте

### Использование ресурсов:

1. Вкладка **"Metrics"**
2. Графики CPU, RAM, Network
3. Стоимость в реальном времени

---

## 🔄 Автоматические обновления

Railway автоматически обновляет бота при push в GitHub!

```bash
# На локальном компьютере:
git add .
git commit -m "Добавлена новая функция"
git push origin main

# Railway автоматически:
# 1. Обнаружит изменения
# 2. Пересоберет Docker образ
# 3. Перезапустит бота
# ✅ Готово!
```

### Отключить автодеплой:

1. Settings → Deploy Triggers
2. Выключите "Automatic Deploys"
3. Теперь нужно деплоить вручную через кнопку "Deploy"

---

## ⚙️ Дополнительные настройки

### Настройка времени рестарта:

По умолчанию бот перезапускается при падении. Изменить можно в `railway.toml`:

```toml
[deploy]
restartPolicyType = "ON_FAILURE"  # Или "ALWAYS", "NEVER"
restartPolicyMaxRetries = 10      # Максимум попыток
```

### Увеличение памяти/CPU:

1. Settings → Resources
2. Выберите нужный план
3. Подтвердите изменения

### Настройка доменов (для веб-версии):

1. Settings → Networking
2. Generate Domain
3. Или добавьте свой домен

---

## 🔐 Безопасность

### ✅ Рекомендации:

1. **Никогда не коммитьте .env файл**
   ```bash
   # Убедитесь что .env в .gitignore
   echo "data/.env" >> .gitignore
   ```

2. **Используйте переменные окружения Railway**
   - Все секреты в Variables (не в коде!)
   - BOT_TOKEN, API_ID, API_HASH и т.д.

3. **Ограничьте доступ к репозиторию**
   - Сделайте репозиторий приватным (Settings → Danger Zone → Change visibility)

4. **Включите 2FA на GitHub**
   - Settings → Password and authentication → Two-factor authentication

---

## 💾 База данных

### Railway автоматически создает volume для данных:

Railway сохраняет папку `data/` между перезапусками:

```
data/
├── telegram_analyzer.db  # База данных SQLite
└── users/                # Сессии пользователей
    ├── 123456/
    └── 789012/
```

### Backup базы данных:

**Вариант 1: Через Railway CLI**
```bash
# Установить Railway CLI
npm i -g @railway/cli

# Войти
railway login

# Подключиться к проекту
railway link

# Скачать файлы
railway run bash -c "cat data/telegram_analyzer.db" > backup.db
```

**Вариант 2: Автоматический backup (позже)**
Можно настроить автоматический backup в облако (S3, Google Drive, etc.)

---

## ⚠️ Решение проблем

### Бот не запускается:

**1. Проверьте логи:**
   - Railway → Logs
   - Ищите красные ошибки

**2. Проверьте переменные окружения:**
   - Variables → убедитесь что BOT_TOKEN установлен
   - Нет лишних пробелов в токене

**3. Проверьте сборку:**
   - Deployments → Build Logs
   - Все ли зависимости установлены?

### "ModuleNotFoundError":

Проблема с зависимостями в `requirements.txt`:

1. Проверьте что все зависимости указаны
2. Убедитесь что нет опечаток
3. Пересоберите проект: Deployments → "Redeploy"

### Бот не отвечает:

1. **Проверьте BOT_TOKEN:**
   ```
   Variables → BOT_TOKEN → правильный ли?
   ```

2. **Проверьте логи:**
   ```
   Logs → ищите ошибки авторизации
   ```

3. **Проверьте статус бота:**
   - Найдите бота в Telegram
   - Убедитесь что он активен (не забанен)

### "Out of credits":

Кредиты закончились:

1. Settings → Usage
2. Посмотрите сколько потрачено
3. Добавьте платежный метод или перенесите на другой сервис

---

## 📈 Оптимизация стоимости

### Как сократить расходы:

1. **Уменьшить ресурсы**
   - Settings → Resources → Выберите меньший план

2. **Остановить в неактивное время** (если бот не нужен 24/7)
   ```bash
   # Через Railway CLI
   railway down  # Остановить
   railway up    # Запустить
   ```

3. **Использовать спящий режим** (платные планы)
   - Settings → Sleep Mode
   - Автоматическая остановка при бездействии

---

## 🔄 Миграция с Railway на VPS

Если хотите перенести на свой сервер:

1. **Экспорт данных:**
   ```bash
   railway run bash -c "tar -czf backup.tar.gz data/"
   railway run bash -c "cat backup.tar.gz" > backup.tar.gz
   ```

2. **Следуйте инструкции:**
   - См. `docs/SERVER_DEPLOYMENT.md`
   - Используйте те же переменные окружения

---

## ✅ Чеклист развертывания

- [ ] Создал аккаунт на Railway
- [ ] Подключил GitHub репозиторий
- [ ] Создал проект на Railway
- [ ] Настроил переменную BOT_TOKEN
- [ ] Дождался успешного деплоя (Status: Success)
- [ ] Проверил логи (видно "Бот запущен")
- [ ] Протестировал бота в Telegram (/start)
- [ ] Настроил автообновления (git push)
- [ ] (Опционально) Настроил мониторинг
- [ ] (Опционально) Сделал backup базы данных

---

## 🎯 Готово!

Ваш бот работает на Railway! 🎉

**Что дальше:**
- Следите за логами: Railway → Logs
- Мониторьте использование: Railway → Metrics
- Обновляйте бота: `git push origin main`

**Полезные ссылки:**
- [Railway Dashboard](https://railway.app/dashboard)
- [Railway Docs](https://docs.railway.app)
- [Railway CLI](https://docs.railway.app/develop/cli)
- [Railway Discord](https://discord.gg/railway)

---

## 🆘 Поддержка

**Если что-то не работает:**

1. Проверьте логи в Railway
2. Проверьте переменные окружения
3. Попробуйте Redeploy
4. Посмотрите `docs/TROUBLESHOOTING.md`

**Альтернативные платформы:**
- Render: https://render.com
- Fly.io: https://fly.io
- Koyeb: https://koyeb.com
- Heroku: https://heroku.com (платно)

---

**Удачи с развертыванием!** 🚀
