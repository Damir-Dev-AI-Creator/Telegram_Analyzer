# 🚀 Ysell Analyzer v0.2.0

Приложение для экспорта и анализа сообщений из Telegram чатов с использованием **Claude API (Anthropic)**.

## ✨ Особенности

- 📱 Экспорт сообщений из любого Telegram чата
- 🤖 Автоматический анализ с помощью Claude API
- 📄 Генерация профессиональных DOCX отчётов
- 🖥️ Кросс-платформенность (Windows, macOS, Linux)
- 🎨 Современный GUI на CustomTkinter
- ⌨️ Поддержка Cmd+V на macOS, Ctrl+V на Windows/Linux

## 📋 Требования

- Python 3.9 или выше
- Telegram API credentials (получите на https://my.telegram.org/apps)
- Claude API key (получите на https://console.anthropic.com/settings/keys)

## 🔧 Установка

```bash
# Клонирование или скачивание
cd ysell_analyzer

# Установка зависимостей
pip install -r requirements.txt

# Запуск
python main.py
```

## 🚀 Использование

### GUI режим (по умолчанию)
```bash
python main.py
```

### Консольный режим
```bash
python main.py --console
```

### Инициализация (создание папок)
```bash
python main.py --init
```

### Показать пути
```bash
python main.py --paths
```

## 📁 Структура файлов

После установки создаются следующие директории:

| ОС | Конфигурация | Рабочие файлы |
|----|--------------|---------------|
| **Windows** | `%APPDATA%\YsellAnalyzer` | `Documents\YsellAnalyzer` |
| **macOS** | `~/Library/Application Support/YsellAnalyzer` | `~/Documents/YsellAnalyzer` |
| **Linux** | `~/.config/YsellAnalyzer` | `~/Documents/YsellAnalyzer` |

### Структура рабочих файлов:
```
YsellAnalyzer/
├── input_csv/      # CSV файлы для анализа
└── output_docx/    # Результаты анализа
```

## 🔑 Горячие клавиши

| Действие | Windows/Linux | macOS |
|----------|---------------|-------|
| Вставить | Ctrl+V | ⌘+V |
| Копировать | Ctrl+C | ⌘+C |
| Вырезать | Ctrl+X | ⌘+X |
| Выделить всё | Ctrl+A | ⌘+A |

## 📦 Сборка standalone

### PyInstaller (рекомендуется)
```bash
pip install pyinstaller
pyinstaller --name="YsellAnalyzer" --windowed --onefile main.py
```

### Nuitka (оптимизированная)
```bash
pip install nuitka
nuitka --standalone --onefile --enable-plugin=tk-inter main.py
```

## 🛡️ Безопасность

- Все credentials хранятся локально в файле `.env`
- Session файлы Telegram хранятся в защищённой директории приложения
- Никакие данные не передаются на внешние серверы (кроме API запросов)
- API ключ Claude не логируется и не сохраняется в отчётах

## 📝 Зависимости

```
telethon>=1.34.0       # Telegram API
pandas>=2.0.0          # Обработка данных
anthropic>=0.40.0      # Claude API
python-docx>=1.1.0     # Создание DOCX
customtkinter>=5.2.0   # GUI
Pillow>=10.0.0         # Изображения
python-dotenv>=1.0.0   # Переменные окружения
```

## 📝 Лицензия

MIT License
