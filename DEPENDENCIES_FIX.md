# 🔧 Решение проблем с установкой зависимостей

Если при развертывании возникают ошибки установки зависимостей, следуйте этому руководству.

---

## 🚨 Типичные ошибки

### 1. "ERROR: Could not find a version that satisfies the requirement"
**Причина:** Указана слишком новая версия пакета, которая еще недоступна

**Решение:** Используйте `requirements-server.txt` (уже исправлен с более старыми версиями)

### 2. "ModuleNotFoundError: No module named 'xxx'"
**Причина:** Пакет не установлен или установка не завершилась

**Решение:** Проверьте Build Logs в Railway/вашей платформе

### 3. "error: command 'gcc' failed"
**Причина:** Отсутствуют системные библиотеки для компиляции

**Решение:** Используйте обновленный `Dockerfile` (уже включает все нужное)

---

## ✅ Решения (по порядку)

### Решение 1: Использовать обновленные файлы (Рекомендуется)

Просто используйте последнюю версию из репозитория:
```bash
git pull origin main
```

Файлы уже исправлены:
- ✅ `requirements-server.txt` - стабильные версии
- ✅ `Dockerfile` - добавлены все системные зависимости
- ✅ Обновлен pip перед установкой

### Решение 2: Использовать минимальные требования

Если `requirements-server.txt` не работает, используйте минимальную версию:

**Для Railway:**
1. Откройте проект в Railway
2. Settings → Build
3. Dockerfile Path: измените на `Dockerfile.minimal`
4. Redeploy

**Для локального Docker:**
```bash
docker build -f Dockerfile.minimal -t telegram-analyzer .
```

### Решение 3: Изменить Dockerfile вручную

Откройте `Dockerfile` и замените:
```dockerfile
# Было:
COPY requirements-server.txt .
RUN pip install --no-cache-dir -r requirements-server.txt

# На:
COPY requirements-minimal.txt .
RUN pip install --no-cache-dir -r requirements-minimal.txt
```

### Решение 4: Проверить локально (перед деплоем)

Запустите скрипт проверки:
```bash
./check_dependencies.sh
```

Скрипт покажет какие зависимости установятся, а какие нет.

---

## 🔍 Диагностика

### Проверить что именно не устанавливается:

**Локально:**
```bash
pip install --dry-run -r requirements-server.txt
```

**На Railway:**
1. Deployments → выберите последний деплой
2. Build Logs → найдите строки с ERROR
3. Смотрите какой именно пакет вызывает ошибку

### Частые проблемники:

| Пакет | Проблема | Решение |
|-------|----------|---------|
| `pandas>=2.3.0` | Версия не существует | Используйте `pandas>=2.0.0` |
| `anthropic>=0.76.0` | Слишком новая | Используйте `anthropic>=0.40.0` |
| `cryptography>=43.0.0` | Слишком новая | Используйте `cryptography>=41.0.0` |
| `qrcode` | Нужен Pillow | Добавьте `Pillow>=10.0.0` |

---

## 📝 Структура файлов зависимостей

Проект имеет 3 файла requirements:

### 1. `requirements.txt` (для локальной разработки)
- Включает GUI (customtkinter, Pillow)
- Все функции, включая графический интерфейс
- **Используйте для:** локальный запуск с GUI

### 2. `requirements-server.txt` (для сервера)
- Без GUI зависимостей
- Оптимизированные версии пакетов
- **Используйте для:** Railway, VPS, Docker

### 3. `requirements-minimal.txt` (резервный)
- Минимальный набор без версий
- Гарантированная установка
- **Используйте для:** когда основные файлы не работают

---

## 🐳 Dockerfile варианты

### 1. `Dockerfile` (основной)
- Использует `requirements-server.txt`
- Полный набор системных библиотек
- Оптимизирован для Railway

### 2. `Dockerfile.minimal` (резервный)
- Использует `requirements-minimal.txt`
- Минимальные системные зависимости
- Быстрая сборка

---

## ⚡ Быстрое решение для Railway

Если не работает деплой на Railway:

### Способ 1: Изменить Dockerfile Path
```
Railway Settings → Build → Dockerfile Path → "Dockerfile.minimal"
```

### Способ 2: Добавить buildpacks
```
Railway Settings → Build → Add Buildpack → Python
```

### Способ 3: Изменить команду старта
```
Railway Settings → Deploy → Start Command → "pip install -r requirements-minimal.txt && python main.py --bot"
```

---

## 🆘 Если ничего не помогает

### План Б: Установка по одному пакету

Создайте файл `install_deps.sh`:
```bash
#!/bin/bash
pip install telethon
pip install pandas
pip install python-dotenv
pip install anthropic
pip install python-docx
pip install aiogram
pip install sqlalchemy[asyncio]
pip install aiosqlite
pip install cryptography
pip install qrcode
pip install Pillow
```

В Dockerfile:
```dockerfile
COPY install_deps.sh .
RUN chmod +x install_deps.sh && ./install_deps.sh
```

---

## ✅ Проверка успешной установки

После деплоя проверьте что всё установлено:

**Railway:**
```
Logs → ищите "Successfully installed"
```

**Локально:**
```bash
docker run telegram-analyzer python -c "import telethon, pandas, anthropic, aiogram; print('OK')"
```

Если видите "OK" - всё работает!

---

## 📞 Поддержка

Если проблемы продолжаются:

1. Проверьте `docs/RAILWAY_DEPLOYMENT.md` раздел "Решение проблем"
2. Проверьте `docs/TROUBLESHOOTING.md`
3. Запустите `./check_dependencies.sh` локально
4. Посмотрите Build Logs в Railway

---

## 🎯 Итоговая рекомендация

**Используйте последние версии файлов из репозитория:**

```bash
git pull origin main
```

Всё уже настроено и протестировано! ✅
