# Ysell Analyzer - Dependencies
# Install: pip install -r requirements.txt

# Core
telethon>=1.42.0           # Telegram API client (updated for latest features)
pandas>=2.3.0              # Data processing (security fix: CVE-2024-9880)
python-dotenv>=1.2.0       # Environment variables (Python 3.14 support)

# AI Integration (Claude API)
anthropic>=0.76.0          # Claude API client (critical update for Claude Opus 4.5)

# Document Generation
python-docx>=1.2.0         # DOCX creation (latest stable)

# GUI
customtkinter>=5.2.2       # Modern Tkinter UI (latest version, maintenance inactive)
Pillow>=12.0.0             # Image support for CTk (security patches, CVE-2025-48379)

# Telegram Bot
aiogram>=3.15.0            # Async Telegram Bot framework

# Database (Multi-User Support)
sqlalchemy[asyncio]>=2.0.36  # Async ORM for database
aiosqlite>=0.20.0            # Async SQLite driver
cryptography>=43.0.3         # Session encryption
qrcode>=8.0                  # QR code generation for Telegram auth

# Optional: Enhanced UX
# tkcalendar>=1.6.1        # Date picker widget
# tkinterdnd2>=0.3.0       # Drag & drop support

# Optional: Security
# keyring>=24.0.0          # Secure credential storage
# cryptography>=41.0.0     # Session encryption

# Development
# pyinstaller>=6.0.0       # Standalone builds
# nuitka>=2.0.0            # Optimized builds
